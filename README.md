# tipout

Surfing Deer tip-out automation. Deterministic CLI that reads POS Excel workbooks and produces payroll-ready reports.

## Install
```
py -m venv .venv
.venv/Scripts/pip install -e ".[dev]"
```
## Usage
```
tipout --help
```
